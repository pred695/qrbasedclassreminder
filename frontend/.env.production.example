# Production Environment Variables for Frontend (Vercel)

# Backend API URL
# OPTION 1 (Recommended): Use relative URL with Vercel rewrites
# This proxies API requests through the frontend domain, solving cookie/CORS issues
# Set this to empty string to use relative URLs (e.g., /api/admin/login)
# Make sure to update vercel.json with your actual backend URL
VITE_API_BASE_URL=

# OPTION 2: Use direct backend URL
# Only use this if you're NOT using Vercel rewrites
# VITE_API_BASE_URL=https://your-backend.vercel.app

# Note: When using OPTION 1, make sure:
# 1. Update frontend/vercel.json with your backend URL
# 2. Backend must set sameSite="none" and secure=true for cookies
# 3. Backend COOKIE_DOMAIN should be empty or not set
